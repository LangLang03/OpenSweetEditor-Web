# Demo.Android

Android 端 Avalonia 示例工程。

## 说明

- 与桌面 `Demo` 共用 `Demo.Shared` 示例逻辑。
- 通过 `DemoPlatformServices` 注入 Android IME 可视区域适配。
- Native runtime 仍按仓库 `prebuilt/android/*/libsweeteditor.so` 路径收口。
