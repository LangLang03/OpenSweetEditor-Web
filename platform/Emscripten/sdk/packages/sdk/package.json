{
  "name": "@sweeteditor/sdk",
  "version": "0.0.5-rc.1",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./runtime/*": "./runtime/*"
  },
  "files": [
    "dist",
    "runtime"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node ../../scripts/prepare-sdk-runtime.mjs",
    "typecheck": "pnpm --filter @sweeteditor/core run build && pnpm --filter @sweeteditor/widget run build && tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "pnpm --filter @sweeteditor/core run build && pnpm --filter @sweeteditor/widget run build && vitest run"
  },
  "dependencies": {
    "@sweeteditor/core": "workspace:*",
    "@sweeteditor/widget": "workspace:*"
  }
}

