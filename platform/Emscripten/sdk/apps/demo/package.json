{
  "name": "@sweeteditor/demo",
  "version": "0.0.5-rc.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "typecheck": "pnpm --filter @sweeteditor/core run build && pnpm --filter @sweeteditor/widget run build && pnpm --filter @sweeteditor/sdk run build && pnpm --filter @sweeteditor/providers-sweetline run build && tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@sweeteditor/providers-sweetline": "workspace:*",
    "@sweeteditor/sdk": "workspace:*"
  }
}

